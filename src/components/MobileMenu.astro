---
interface NavItem {
  label: string;
  href: string;
  section: string;
  external?: boolean;
}

interface Props {
  navItems: NavItem[];
  secondaryNavItems: NavItem[];
  tertiaryNavItems: NavItem[];
  activeSection?: string;
}

const { navItems, secondaryNavItems, tertiaryNavItems, activeSection = '' } = Astro.props;

const externalArrowSvg = `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" class="inline-block w-6 h-6 ml-[-10px] mb-[3px]"><line x1="7" y1="17" x2="17" y2="7"></line><polyline points="7 7 17 7 17 17"></polyline></svg>`;
---

<div
  id="mobile-menu-overlay"
  class="absolute inset-0 z-40 bg-ramper-white opacity-0 pointer-events-none transition-opacity duration-300 lg:hidden"
  aria-hidden="true"
>
  <nav class="flex flex-col items-end pr-8 pt-[160px] h-full overflow-y-auto">
    <ul class="list-none space-y-6 text-[36px] leading-[1.2] font-normal text-right">
      {navItems.map((item) => (
        <li>
          <a
            href={item.href}
            class:list={[
              'block transition-colors hover:text-ramper-blue-mid mobile-nav-link nav-link-interactive',
              activeSection === item.section
                ? 'font-bold text-ramper-blue-deep'
                : 'text-ramper-black'
            ]}
            {...(item.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
            {...(activeSection === item.section && !item.external ? { 'aria-current': 'page' } : {})}
          >
            {item.label.replace('↗', '')}
            {item.label.includes('↗') && (
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" class="inline-block w-6 h-6 ml-[-10px] mb-[3px]">
                <line x1="7" y1="17" x2="17" y2="7"></line>
                <polyline points="7 7 17 7 17 17"></polyline>
              </svg>
            )}
          </a>
        </li>
      ))}

      {/* About section */}
      <li class="mt-10">
        {secondaryNavItems.map((item) => (
          <a
            href={item.href}
            class:list={[
              'block transition-colors hover:text-ramper-blue-mid mobile-nav-link',
              activeSection === item.section
                ? 'font-bold text-ramper-blue-deep'
                : 'text-ramper-black'
            ]}
            {...(activeSection === item.section ? { 'aria-current': 'page' } : {})}
          >
            {item.label}
          </a>
        ))}
      </li>

      {/* Contact section */}
      <li class="mt-10">
        {tertiaryNavItems.map((item) => (
          <a
            href={item.href}
            class:list={[
              'block transition-colors hover:text-ramper-blue-mid mobile-nav-link',
              activeSection === item.section
                ? 'font-bold text-ramper-blue-deep'
                : 'text-ramper-black'
            ]}
            {...(activeSection === item.section ? { 'aria-current': 'page' } : {})}
          >
            {item.label}
          </a>
        ))}
      </li>
    </ul>
  </nav>
</div>

<script>
  document.addEventListener('astro:page-load', () => {
    const toggle = document.getElementById('mobile-menu-toggle');
    const overlay = document.getElementById('mobile-menu-overlay');
    const hamburger = document.getElementById('hamburger-icon');
    const closeIcon = document.getElementById('close-icon');

    if (!toggle || !overlay || !hamburger || !closeIcon) return;

    function openMenu() {
      overlay!.classList.remove('opacity-0', 'pointer-events-none');
      overlay!.classList.add('opacity-100');
      overlay!.setAttribute('aria-hidden', 'false');
      hamburger!.classList.add('hidden');
      closeIcon!.classList.remove('hidden');
      toggle!.setAttribute('aria-expanded', 'true');
      toggle!.setAttribute('aria-label', 'Cerrar menú');
    }

    function closeMenu() {
      overlay!.classList.add('opacity-0', 'pointer-events-none');
      overlay!.classList.remove('opacity-100');
      overlay!.setAttribute('aria-hidden', 'true');
      hamburger!.classList.remove('hidden');
      closeIcon!.classList.add('hidden');
      toggle!.setAttribute('aria-expanded', 'false');
      toggle!.setAttribute('aria-label', 'Abrir menú');
    }

    toggle.addEventListener('click', () => {
      const isOpen = toggle.getAttribute('aria-expanded') === 'true';
      isOpen ? closeMenu() : openMenu();
    });

    overlay.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', closeMenu);
    });

    closeMenu();
  });
</script>
