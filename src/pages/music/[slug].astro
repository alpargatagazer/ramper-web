---
// src/pages/music/[slug].astro
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection, render } from 'astro:content';
import { Picture } from 'astro:assets';
import { typeLabels } from '@/constants';
import { formatDate } from '@/utils/dates';
import ExternalLink from '@/components/ExternalLink.astro';
import BackLink from '@/components/BackLink.astro';

export async function getStaticPaths() {
  const releases = await getCollection('releases');
  return releases.map((release) => ({
    params: { slug: release.id },
    props: { release },
  }));
}

const { release } = Astro.props;
const { Content } = await render(release);

---

<BaseLayout title={`${release.data.title} — Ramper`} activeSection="music">
  <div class="page-container">

    <div class="content-overlay p-8 md:p-12">

    {/* Header: Cover + metadata side by side */}
    <header class="flex flex-col md:flex-row gap-8 mb-12 items-start">
      {release.data.coverImage ? (
        <a 
          href={release.data.coverImage.src} 
          target="_blank" 
          title="Ver arte a tamaño completo"
          class="relative block w-full md:w-[280px] h-auto aspect-square flex-shrink-0 noise-overlay overflow-hidden cursor-zoom-in group"
        >
          <Picture
            src={release.data.coverImage}
            alt={`Portada de ${release.data.title}`}
            class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-102"
            widths={[300, 600, 1200]}
            sizes="(min-width: 768px) 280px, 100vw"
            loading="eager"
            decoding="sync"
            fetchpriority="high"
            quality={70}
            formats={['avif', 'webp']}
            transition:name={`release-cover-${release.id}`}
          />
          <div class="absolute inset-0 bg-ramper-black/20 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center pointer-events-none">
            <span class="text-ramper-white text-xs font-bold uppercase tracking-widest">Ver original</span>
          </div>
        </a>
      ) : (
        <div class="w-[280px] aspect-square bg-ramper-black/10 flex items-center justify-center text-xs text-ramper-black/40 uppercase tracking-widest flex-shrink-0">
          Cover
        </div>
      )}

      <div class="flex flex-col justify-center">
        <h1 class="text-4xl font-bold tracking-tight text-ramper-black mb-3">
          {release.data.title}
        </h1>

        <p class="flex items-center gap-3 text-ramper-black/70 mb-6">
          <span class="text-xs font-bold uppercase tracking-wider border border-ramper-black/30 px-2 py-1">
            {typeLabels[release.data.type]}
          </span>
          <time datetime={release.data.releaseDate}>
            {formatDate(release.data.releaseDate)}
          </time>
        </p>

        <div class="flex gap-3 flex-wrap">
          {/* Main Actions */}
          {release.data.storeUrl && (
            <ExternalLink href={release.data.storeUrl} variant="primary">
              Comprar
            </ExternalLink>
          )}
          {release.data.bandcampUrl && (
            <ExternalLink href={release.data.bandcampUrl} variant="secondary">
              Bandcamp
            </ExternalLink>
          )}

          {/* Streaming Platforms */}
          {release.data.spotifyUrl && (
            <ExternalLink href={release.data.spotifyUrl} variant="outline">
              Spotify
            </ExternalLink>
          )}
          {release.data.appleMusicUrl && (
            <ExternalLink href={release.data.appleMusicUrl} variant="outline">
              Apple Music
            </ExternalLink>
          )}
          {release.data.tidalUrl && (
            <ExternalLink href={release.data.tidalUrl} variant="outline">
              Tidal
            </ExternalLink>
          )}
          {release.data.qobuzUrl && (
            <ExternalLink href={release.data.qobuzUrl} variant="outline">
              Qobuz
            </ExternalLink>
          )}
        </div>
      </div>
    </header>

    {/* Body content (credits, description) */}
    {/* Reduced line spacing for list items specifically for credits */}
    <div class="prose-ramper prose-ul:my-2 prose-li:my-0 mb-12">
      <Content />
    </div>

    {/* Tracklist & Lyrics */}
    {release.data.tracks && release.data.tracks.length > 0 && (
      <div class="mb-12">
        <h2 class="section-title">Tracklist</h2>
        <ul class="space-y-1">
          {release.data.tracks.map((track, idx) => (
            <li class="border-b border-ramper-black/10 last:border-0">
              {track.lyrics ? (
                <details class="group">
                  <summary class="flex justify-between items-center cursor-pointer list-none text-ramper-black hover:text-ramper-blue-deep transition-colors py-3">
                    <span class="font-bold">{idx + 1}. {track.title}</span>
                    <span class="text-[10px] uppercase tracking-widest opacity-40 group-open:opacity-0 transition-opacity">Letra ↓</span>
                  </summary>
                  <div class="mt-2 mb-8 pl-4 border-l-2 border-ramper-black/10 text-ramper-black/70 whitespace-pre-wrap font-serif italic text-lg leading-relaxed">
                    {track.lyrics.trim()}
                  </div>
                </details>
              ) : (
                <div class="py-3 font-bold text-ramper-black">
                  {idx + 1}. {track.title}
                </div>
              )}
            </li>
          ))}
        </ul>
      </div>
    )}

    {/* Footer: back link */}
    <footer class="pt-8 border-t border-ramper-black/10">
      <BackLink href="/music">← volver a música</BackLink>
    </footer>
    </div>
  </div>
</BaseLayout>
